Test what happens if a service needs to get recovered but select_service_node
cannot return any possible node.

Try recovery forever, as this is HA and its single job is to get that service
available again, even if trying forever.
